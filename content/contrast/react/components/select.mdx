---
title: "Select"
metaTitle: "Select Component for React Bootstrap"
metaDescription: "React Bootstrap Select is a very useful component that the `↑` and `↓` arrow keys can be used to browse through options and the `↵` key to select the required option (works for stateful select)."

---

import React from "react";

# React Bootstrap Select

When selected, React Bootstrap Select displays a collapsible list of numerous options, and it may be used in forms, menus, and surveys.

React Bootstrap Select is a very useful component that the `↑` and `↓` arrow keys can be used to browse through options and the `↵` key to select the required option (works for stateful select).

## Importing the React Bootstrap Select Component

For you to use the React Bootstrap Select components, you will need to import `CDBSelect` into your project.

```javascript
import { CDBSelect } from "cdbreact";
```

## CDB Select 

The `CDBSelect` prop takes in an `option` prop whose value is an array of different user options.

<div class="container">
  <select class="w-100 p-2">
    <option>Choose an Option</option>
    <option value="1">Option 1</option>
    <option value="2">Option 2</option>
    <option value="3">Option 3</option>
  </select>
</div>

```javascript
import React, { useState } from "react";
import { CDBSelect, CDBContainer } from "cdbreact";

export const Select = () => {
const [option] = useState([
    {
      text: "Option 1",
      value: "1",
    },
    {
      text: "Option 2",
      value: "2",
    },
    {
      text: "Option 3",
      value: "3",
    },
  ]);

  return (
    <CDBContainer>
        <CDBSelect options={option} selected="Choose an option"/>
    </CDBContainer>
  );
};
```

## API Reference: Contrast React Bootstrap Select Props

This section will elaborate on the props available with the Contrast React Bootstrap Select component. You'll learn what these props do, how to utilize them, and what their default values are.

You can check other props options of the `CDBSelect` on the table below.

| Name            | Type        | Default      |   Description| Example      |
| :------------- | :----------: | -----------: | :----------: | -----------: |
| className      | String       | Adds custom classes             |Adds custom classes	      |     <CDBSelect className="myClass" ... />  |
| tag            | String       | div          | Changes default tag | <CDBSelect tag="input" ... />  |
| selected       | String       |              | Set default select text content. | <CDBSelect2 selected="Choose one" ... />  |
| options        | Array of Objects |              | Sets options array as source of data. This property is used in alternative `Select****2` version. | <CDBSelect2 options={options}" ... />  |
